query Workload($projectId: ID) {
  workload(projectId: $projectId) {
    assigneeId
    totalEstimatedHours
    totalActualHours
    openTaskCount
  }
  users {
    ...UserSummaryFields
  }
  projects {
    id
    name
  }
}
