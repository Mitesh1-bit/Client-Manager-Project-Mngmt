query Workload($projectId: ID) {
  workload(projectId: $projectId) {
    assigneeId
    totalEstimatedHours
    totalActualHours
    openTaskCount
    projectCount
    clientCount
  }
  users {
    ...UserSummaryFields
  }
  projects {
    id
    name
  }
}
